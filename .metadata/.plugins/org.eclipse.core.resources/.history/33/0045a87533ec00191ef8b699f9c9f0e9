
public class Insercion {
public int[] ordenar(int vec[]) {
	
	
	for(int i=1;i<vec.length;i++) {
		int j=i-1;
		int aux;
		while(j<=0 && vec[j]>vec[i]) {
			aux=vec[i];
			vec[i]=vec[j];
			vec[j]=aux;
			j--;
		}
		
	}
	return vec;
}
}
